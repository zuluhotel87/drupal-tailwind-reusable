<div{{ attributes.addClass('space-y-8 md:grid md:grid-cols-2 md:gap-12 md:space-y-0') }}>
  {% for item in items %}
    <div{{ item.attributes }}>
      {{ item.content }}
    </div>
  {% endfor %}
</div>
