{% from "@shizen/macros/ui/atoms/link/ui.link.html.twig" import link %}

{% if page.footer %}
  <footer class="bg-light border-t border-muted-light">
    <div class="container flex items-center py-10">
      <span>© {{ 'now'|date('Y') ~ ' |' }}</span>
      <span class="mx-1"> {{ link({
          label: site_name,
          href: path('<front>'),
          attributes: 'rel="home"'
        }) }}</span>
      <span>{{ '- ' ~ 'All Rights Reserved'|t }}.</span>

      {{ page.footer }}
    </div>
  </footer>
{% endif %}
