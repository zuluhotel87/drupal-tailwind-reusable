(function(Drupal,Backbone){Drupal.contextual.RegionView=Backbone.View.extend({events(){let touchStart=!1;return{touchstart(){touchStart=!0},mouseenter(){if(!touchStart){this.model.set('regionIsHovered',!0)}},mouseleave(){if(!touchStart){this.model.close().blur().set('regionIsHovered',!1)}},mousemove(){touchStart=!1},}},initialize(){this.listenTo(this.model,'change:hasFocus',this.render)},render(){this.$el.toggleClass('focus',this.model.get('hasFocus'));return this},},)})(Drupal,Backbone)