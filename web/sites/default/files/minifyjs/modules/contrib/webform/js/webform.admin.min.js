(function($,Drupal,debounce,once){Drupal.behaviors.webformFilterAutocomplete={attach:function(context){$(once('webform-autocomplete','.webform-filter-form input.form-autocomplete',context)).each(function(){if(/\(([^)]+)\)$/.test(this.value)){this.value=this.defaultValue}
$(this).bind('autocompleteselect',function(event,ui){if(ui.item){$(this).val(ui.item.value);$(this.form).trigger('submit')}})})}};Drupal.behaviors.webformTableRowHref={attach:function(context){$(once('webform-results-table','.webform-results-table',context)).on('click',function(event){if(event.target.tagName==='A'||event.target.tagName==='BUTTON'||event.target.tagName==='INPUT'){return!0}
if($(event.target).parents('a[href]').length||$(event.target).parents('.dropbutton-widget').length){return!0}
var $input=$(event.target).closest('td').find('input');if($input.length){if($input.attr('type')==='checkbox'){$input.click()}
return!0}
var $tr=$(event.target).parents('tr[data-webform-href]');if(!$tr.length){return!0}
window.location=$tr.attr('data-webform-href');return!1})}}})(jQuery,Drupal,Drupal.debounce,once)