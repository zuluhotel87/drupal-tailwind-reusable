(function($,Drupal,once){Drupal.behaviors.webformRating={attach:function(context){$(once('webform-rating','[data-rateit-backingfld]',context)).each(function(){var $rateit=$(this);var $input=$($rateit.attr('data-rateit-backingfld'));if(!$.fn.rateit){$rateit.remove();$input.removeClass('js-webform-visually-hidden');return}
if(document.readyState==='complete'){$rateit.rateit()}else{window.setTimeout(function(){$rateit.rateit()})}
$input.on('change',function(){$rateit.rateit('value',$input.val())});$input.on('webform:disabled',function(){$rateit.rateit('readonly',$input.is(':disabled'))})})}}})(jQuery,Drupal,once)