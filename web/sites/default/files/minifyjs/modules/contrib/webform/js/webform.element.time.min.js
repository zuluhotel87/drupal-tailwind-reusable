(function($,Drupal,once){Drupal.webform=Drupal.webform||{};Drupal.webform.timePicker=Drupal.webform.timePicker||{};Drupal.webform.timePicker.options=Drupal.webform.timePicker.options||{};Drupal.behaviors.webformTime={attach:function(context,settings){if(!$.fn.timepicker){return}
$(once('webformTimePicker','input[data-webform-time-format]',context)).each(function(){var $input=$(this);var options={};options.timeFormat=$input.data('webformTimeFormat');if($input.attr('min')){options.minTime=$input.attr('min')}
if($input.attr('max')){options.maxTime=$input.attr('max')}
if($input.attr('step')&&($input.attr('step')>60)){options.step=Math.round($input.attr('step')/60)}else{options.step=1}
$input.attr('step','any');options=$.extend(options,Drupal.webform.timePicker.options);$input.timepicker(options)})}}})(jQuery,Drupal,once)