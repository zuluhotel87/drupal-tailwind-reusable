(function($,Drupal,once){Drupal.webform=Drupal.webform||{};Drupal.webform.choices=Drupal.webform.choices||{};Drupal.webform.choices.options=Drupal.webform.choices.options||{};Drupal.webform.choices.options.selectSearchMinItems=10;Drupal.behaviors.webformChoices={attach:function(context){if(!window.Choices){return}
$(once('webform-choices','select.js-webform-choices, .js-webform-choices select',context)).each(function(){var $select=$(this);var options={shouldSort:!1,loadingText:Drupal.t('Loading...'),noResultsText:Drupal.t('No results found'),noChoicesText:Drupal.t('No choices to choose from'),itemSelectText:Drupal.t('Press to select'),addItemText:function addItemText(value){return Drupal.t('Press Enter to add <b>@value</b>',{'@value':value})},maxItemText:function maxItemText(maxItemCount){return Drupal.t('Only @max value can be added',{'@max':maxItemCount})}};if($select.attr('multiple')){options.removeItemButton=!0}
options=$.extend(options,Drupal.webform.choices.options);if($select.data('placeholder')){options.placeholder=!0;options.placeholderValue=$select.data('placeholder')}
if($select.data('limit')){options.maxItemCount=$select.data('limit')}
var choices=new Choices(this,options);$(this).data('choices',choices)})}};var $document=$(document);$document.on('state:disabled',function(e){var $choices=$(e.target).find('.js-webform-choices');if($(e.target).hasClass('js-webform-choices')){$choices.add(e.target)}
$choices.each(function(){var choices=$(this).data('choices');if(choices){choices[(e.value)?'disable':'enable']()}})})})(jQuery,Drupal,once)