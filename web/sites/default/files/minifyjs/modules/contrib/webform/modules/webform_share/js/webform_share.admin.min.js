(function($,Drupal,once){Drupal.behaviors.webformShareAdminCopy={attach:function(context){$(once('webform-share-admin-copy','.js-webform-share-admin-copy',context)).each(function(){var $container=$(this);var $textarea=$container.find('textarea');var $button=$container.find(':submit, :button');var $message=$container.find('.webform-share-admin-copy-message');$button.on('click',function(){if(window.navigator.clipboard){window.navigator.clipboard.writeText($textarea.val())}
$message.show().delay(1500).fadeOut('slow');Drupal.announce(Drupal.t('Code copied to clipboard…'));return!1})})}}})(jQuery,Drupal,once)